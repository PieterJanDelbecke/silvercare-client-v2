import styled from "@emotion/styled";

const Container = styled.div`
	display: flex;
	justify-content: center;
	align-items: center;
`;
const ResidentForm = () => {
	return <Container>Resident Form</Container>;
};

export default ResidentForm;
